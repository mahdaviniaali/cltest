# تعریف پروژه

```yaml
---
domain: shared
authority: L5
maturity: working
owner: project-definition
intent: explain
source: Technical Interview Task — Bama.ir
questions:
  - این پروژه دقیقاً چیست؟
  - چه قابلیت‌هایی in-scope هستند؟
  - مرز هر subsystem کجاست؟
  - حداقل قرارداد داده advertisement چیست؟
not_authoritative_for:
  - انتخاب تکنولوژی (→ رویکرد_پروژه.md — هنوز تصمیم‌گیری نشده)
  - رفتار shipped code (→ */current_state/)
  - معماری freeze‌شده (→ */architecture/, */decisions/)
---
```

**Document intent:** Explain. تعریف رسمی تسک — **بدون** lock تکنولوژی یا معماری.

## One-liner

> **Bama.ir New Ads Crawler & Notification System** — سرویسی که آگهی‌های جدید خودرو در Bama.ir را monitor می‌کند، deduplicate می‌کند، با فیلترهای کاربر match می‌کند، و notification ارسال می‌کند.

## Overview

| محور | توضیح |
|---|---|
| **هدف تسک** | ارزیابی طراحی و پیاده‌سازی backend production-oriented |
| **Target site** | [Bama.ir](https://bama.ir) — آگهی‌های خودرو |
| **Trigger** | آگهی **جدید** publish شده — نه re-crawl کل سایت |
| **Consumer** | کاربرانی با search/filter تعریف‌شده |

### مهارت‌های مورد ارزیابی

- Web crawling
- Data extraction & normalization
- Deduplication
- Background / async processing
- User-defined filters
- Notification delivery
- API design
- Persistence
- Containerization
- Kubernetes deployment
- Observability
- Technical decision-making

> انتخاب زبان، framework، DB، queue، و infra **آزاد** است — در [`رویکرد_پروژه.md`](رویکرد_پروژه.md) بعداً تصمیم گرفته می‌شود.

---

## System Context (logical)

```text
                    ┌─────────────────────┐
                    │  User (API client)   │
                    │  CRUD searches       │
                    └──────────┬──────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         v                     v                     v
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Crawler         │  │  Matching Engine │  │  Notification   │
│  (periodic)      │  │                  │  │  (≥1 channel)   │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                     │
         └─────────────────────┼─────────────────────┘
                               v
                    ┌─────────────────────┐
                    │  Persistence         │
                    │  ads · searches ·    │
                    │  match · notify log  │
                    └─────────────────────┘
                               │
                               v
                         Bama.ir (source)
```

---

## Functional Requirements

### 2.1 Crawling

کرالر باید **به‌صورت دوره‌ای** Bama.ir را برای آگهی‌های **جدید** بررسی کند.

| requirement | detail |
|---|---|
| Extract | اطلاعات relevant هر آگهی |
| Detect new | آگهی‌هایی که قبلاً دیده نشده‌اند |
| Store | ذخیره آگهی‌های کشف‌شده |
| Dedup | بدون duplicate record |
| Resilience | graceful failure handling |
| Schedule | interval قابل تنظیم |

#### حداقل فیلدهای Advertisement

| Field | required | note |
|---|---|---|
| Advertisement ID / unique identifier | ✅ | اگر Bama stable ID نداشت → assumption معقول |
| URL | ✅ | |
| Title | ✅ | |
| Price | ✅ | |
| Brand | ✅ | |
| Model | ✅ | |
| Year | ✅ | |
| Mileage | ✅ | |
| Location | ✅ | |
| Publication time | ✅ | |
| Crawled time | ✅ | timestamp crawl |
| Other useful fields | optional | هر فیلد مفید دیگر |

#### محدودیت‌های قانونی و ethical

- ✅ respect `robots.txt`
- ✅ respect terms of service
- ✅ respect rate limits
- ✅ applicable legal requirements
- ❌ **ممنوع:** bypass anti-bot، دور زدن access control

---

### 2.2 User Management & Filters

کاربران باید بتوانند **search criteria** تعریف کنند.

#### نمونه criteria

| Field | Example |
|---|---|
| Brand | Toyota |
| Model | Corolla |
| Minimum Year | 2020 |
| Maximum Price | 3,000,000,000 |
| Maximum Mileage | 100,000 |
| Location | Tehran |

#### عملیات مورد نیاز

| Operation | required |
|---|---|
| Create search/filter | ✅ |
| Update search/filter | ✅ |
| Delete search/filter | ✅ |
| Enable / disable search/filter | ✅ |
| View configured searches | ✅ |
| Multiple searches per user | ✅ |

#### مثال

```text
User A
  Search #1: Toyota Corolla, Year >= 2020, Price <= 3B
  Search #2: BMW 320,       Year >= 2018, Price <= 5B
```

---

### 2.3 Matching Engine

وقتی آگهی **جدید** کشف شد → تعیین کدام user searches match می‌کنند.

```text
New Ad: Toyota Corolla · 2022 · 80,000 km · 2.8B · Tehran
                    │
                    v
            Matching Engine
                    │
        ┌───────────┼───────────┐
        v           v           v
  User A / S#1 ✓  User B / S#4 ✓  User C / S#7 ✗
```

| requirement | detail |
|---|---|
| Match on new ad | فقط آگهی‌های تازه کشف‌شده |
| No repeat notify | همان ad + همان user + همان search → **یک‌بار** notify |
| Extensible criteria | فیلترها data-driven باشند (نه hard-coded per brand) |

---

### 2.4 Notifications

وقتی match رخ داد → notification به کاربر.

| requirement | detail |
|---|---|
| Minimum channels | **≥ 1** (Email / Telegram / Webhook / Push / …) |
| Content | اطلاعات مفید آگهی + لینک |
| Extensibility | channel جدید **بدون تغییر اساسی** crawler یا matching logic |

#### نمونه محتوای notification

```text
New car advertisement found

Toyota Corolla 2022
Price: 2.8B
Mileage: 80,000 km
Location: Tehran

https://...
```

---

## Logical Subsystems (مرز مسئولیت)

| Subsystem | Job | وابستگی |
|---|---|---|
| **Crawler** | fetch + extract + normalize + dedup + persist ads | Bama.ir, scheduler |
| **Ad Store** | نگهداری آگهی‌ها + dedup key | persistence |
| **User/Search API** | CRUD searches + enable/disable | auth (TBD) |
| **Matching Engine** | new ad → matched searches | ad store, search store |
| **Notification** | deliver + idempotency (no repeat) | matching result |
| **Scheduler** | periodic crawl trigger | configurable interval |

> **تفکیک Platform/Application** و stack تکنولوژی → در رویکرد پروژه (هنوز open).

---

## Non-Functional Requirements (از متن تسک)

| NFR | implied |
|---|---|
| Production-oriented | error handling, logging, deployable |
| Background/async | crawl و notify async |
| Containerization | Docker image |
| Kubernetes | deploy روی K8s |
| Observability | logs, metrics, health (جزئیات TBD) |
| API | REST یا معادل برای user/search management |

---

## In Scope

- Periodic Bama.ir crawl برای آگهی‌های جدید
- Extraction + normalization + deduplication
- User search/filter CRUD
- Matching engine با idempotent notification
- حداقل یک notification channel
- API برای مدیریت searches
- Persistence
- Container + K8s deployment
- Observability baseline

## Out of Scope / Explicit Constraints

| item | reason |
|---|---|
| Anti-bot bypass | ممنوع در spec |
| CAPTCHA circumvention | ممنوع |
| Real-time sub-second alerts | periodic crawl — interval-based |
| Mobile app | not in spec |
| Payment / auth provider choice | TBD in approach |

---

## Data Entities (conceptual — not schema lock)

| Entity | role |
|---|---|
| **Advertisement** | آگهی normalize‌شده از Bama |
| **User** | صاحب searches |
| **Search / Filter** | criteria کاربر + enabled flag |
| **Match** | (ad, search) matched event |
| **Notification** | delivery record — برای idempotency |

---

## Relation to Scaffold Today

| component | cltest today | gap |
|---|---|---|
| Platform crawler core | ✅ `BaseCrawler`, `HttpClient`, `HtmlParser` | Bama-specific parser |
| Storage | ✅ JSON file | DB persistence required |
| Example target | example.com | → Bama.ir |
| User API | ❌ | required |
| Matching | ❌ | required |
| Notification | ❌ | required |
| Scheduler | ❌ | required |
| K8s / Docker | ❌ | required |

---

## Open — intentionally deferred to رویکرد

این موارد **عمداً** در این سند lock نشده‌اند:

→ [`رویکرد_پروژه.md`](رویکرد_پروژه.md) + [`application/spec/open_questions.md`](../application/spec/open_questions.md)

- Stack (language, framework, DB, queue)
- Architecture style (monolith vs services)
- Notification channel اول
- Auth model
- Crawl interval default + rate limit policy
- K8s topology
- Observability stack

---

## Promotion Path

```
working (این سند) → رویکرد تعریف شد → accepted
  → product_blueprint + capability_map update
  → ADR for architectural locks
  → L1 facts after code ships
```
