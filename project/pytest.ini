[pytest]
testpaths = tests
pythonpath = src .
addopts = -m "not stress"
markers =
    stress: heavy load/stress tests (run with: pytest -m stress)
    product: product scenario / acceptance tests (tests/scenarios/)
